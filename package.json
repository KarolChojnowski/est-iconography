{
  "name": "est-iconography",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for the EST iconography package and its Jekyll catalogue.",
  "workspaces": [
    "packages/iconography"
  ],
  "scripts": {
    "clean": "npm run clean --workspace @est/iconography && node tools/clean-catalogue.mjs",
    "validate": "npm run validate --workspace @est/iconography",
    "validate:iconography": "npm run validate --workspace @est/iconography",
    "build:iconography": "npm run build --workspace @est/iconography",
    "bundle:iconography": "npm run build:iconography && node packages/iconography/scripts/build-bundle.mjs",
    "prepare:catalogue": "node tools/prepare-catalogue.mjs",
    "build:catalogue": "npm run build:iconography && npm run prepare:catalogue && cd catalogue && bundle exec jekyll build --destination ../_site",
    "serve:catalogue": "npm run build:iconography && npm run prepare:catalogue && cd catalogue && bundle exec jekyll serve",
    "build": "npm run build:catalogue",
    "test": "npm run validate:iconography && npm run build:iconography && npm run test:bundle --workspace @est/iconography"
  },
  "engines": {
    "node": ">=22"
  }
}
